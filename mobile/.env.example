# Copie para .env e troque pelo IP local da sua máquina.
# Descubra com: hostname -I (Linux) ou ipconfig (Windows).
# Smartphone e PC precisam estar na mesma Wi-Fi.
# Backend: php artisan serve --host=0.0.0.0 --port=8000
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000/api/v1
